import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    token: '' as string,
    userLogged: '' as string
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
